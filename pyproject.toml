[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hanhai-fengxun"
version = "0.1.0"
description = "寒海风巡 V0.1 可解释无人艇抗浪决策原型"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["apps*", "core*", "adapters*"]

